<script> // Mobile menu toggle document.getElementById('menuToggle').addEventListener('click', function() { document.getElementById('menu').classList.toggle('show'); }); // Smooth scrolling for anchor links document.querySelectorAll('a[href^="#"]').forEach(anchor => { anchor.addEventListener('click', function (e) { e.preventDefault(); const targetId = this.getAttribute('href'); if (targetId === '#') return; const targetElement = document.querySelector(targetId); if (targetElement) { window.scrollTo({ top: targetElement.offsetTop - 70, behavior: 'smooth' }); // Close mobile menu if open document.getElementById('menu').classList.remove('show'); } }); }); // Cost Calculator functionality document.getElementById('calculate-btn').addEventListener('click', function() { const bidderCost = parseFloat(document.getElementById('bidder-cost').value) || 0; const connectsCost = parseFloat(document.getElementById('connects-cost').value) || 0; const devCost = parseFloat(document.getElementById('dev-cost').value) || 0; const totalCost = bidderCost + connectsCost + devCost; document.getElementById('total-cost').textContent = '$' + totalCost.toFixed(2); }); // Initialize calculator document.getElementById('calculate-btn').click(); </script> </body> </html>
